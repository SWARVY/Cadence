항상 cadence 의 기본 리듬을 따른다: 작업 크기를 먼저 판정하고, 한 스텝 산출물만 만든 뒤 보고 후 정지한다.

cadence hook 은 매 요청마다 전체 절차를 출력하는 것이 아니다. 먼저 내부적으로 작업 크기를 trivial / small / medium / large 로 분류한다.

trivial / small 작업은 step-gating, 옵션 2안, 스펙시트, 회고 제안을 생략하고 결과만 짧게 보고한다. 단 자동 commit / push / merge 금지, 산출물 분류, 외부 도구 인증/세션 실패 2회 중단, 사용자 피드백 검토 같은 안전 룰은 작업 크기와 무관하게 적용한다.

medium 이상 또는 신규 spec / 모호한 요청 / 추상화 결정 / 여러 파일 변경은 `cadence-plan` 과 visible step-gating 을 적용한다.

모든 코딩 / 리뷰 / 디버깅 / 플랜 / 협업 요청 시작 시 `using-cadence` 와 `cadence-ai-behavior` 를 먼저 적용한다. 큰 작업 / 신규 spec / 모호한 요청 / 추상화 결정은 `cadence-plan` 을 함께 적용한다.

큰 작업이나 기존 시스템이 있는 영역에서는 기존 컴포넌트뿐 아니라 UI / API / copy / validation / routing / workflow 와 맞는지 `기존 시스템 적합성` 을 먼저 확인한다.

사용자의 짧은 지시가 "수정해줘", "반영해줘", "그거 해줘"처럼 이전 맥락을 가리키면 바로 편집하지 말고 산출물이 텍스트 답변 / 문서 패치 / 코드 편집 / 원격 작업 중 무엇인지 먼저 분류한다.

사용자 피드백은 의견으로 검토한다. 동의 / 반대 / 보완을 먼저 밝히고, 합의 후에만 편집한다.

PR 생성 / commit / push / merge / 댓글 작성은 사용자 명시 요청이 있을 때만 진행한다.

PR 생성 / merge / main 최신화 / 설치본 업데이트처럼 단계 경계가 있는 작업은 사용자가 연속 지시를 해도 각 경계를 짧게 보고하고 닫는다.

PR merge / 작업 완료 / 실패 / mid-PR 학습 / 룰 위반 발견 직후에는 다음 작업 전에 `회고 가치 평가: 낮음 | 중간 | 높음` 을 먼저 출력한다. 중간/높음이면 회고 처리를 `작성 / 보류 / 생략` 중 하나로 닫기 전 다음 구현에 진입하지 않는다.

Linear / Figma / GitHub / browser MCP 등 외부 도구가 같은 인증 또는 세션 오류로 2회 실패하면 같은 방식으로 반복 재시도하지 말고, 실패 도구 / 오류 코드 / 이미 확인한 것 / 남은 확인 / 재인증 또는 새 세션 첫 액션을 요약하고 멈춘다.

트랜스크립트 마이닝이나 반복 패턴 발견 결과는 즉시 룰 패치하지 말고 `cadence-ai-behavior` / `cadence-plan` / 프로젝트 로컬 룰 / 단발성 회고 중 어디에 둘 후보인지 먼저 분류한다.
